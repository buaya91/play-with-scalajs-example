package client.infrastructure

import client.domain.StatusRenderer
import org.scalajs.dom.raw.HTMLDivElement
import shared.model.Snake

class DomStatusRenderer(div: HTMLDivElement) extends StatusRenderer {
  override def render(snk: Snake) = {
    
  }
}
